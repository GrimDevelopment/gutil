FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY .. /workspace
WORKDIR /workspace

CMD ["bash", "-lc", "python -m gutil codex-repl"]

